// Import the User and Thought models and export them directly.
module.exports.User = require("./User");
module.exports.Thought = require("./Thought");

// Alternatives

// Import the User and Thought models from their respective files.

// const User = require("./User");
// const Thought = require("./Thought");

// Export the User and Thought models to make them accessible in other parts of the application.

// module.exports = { User, Thought };
